{
  "name": "daily-engine",
  "version": "1.0.0",
  "description": "Daily journal orchestration engine for Obsidian — event-sourced diary with Work/ dual-linking",
  "type": "module",
  "bin": { "daily-engine": "./dist/index.js" },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0",
    "vitest": "^1.0.0"
  }
}
